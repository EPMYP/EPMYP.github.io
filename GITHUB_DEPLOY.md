# GitHub Pages 自动部署指南

## 📋 前置要求

1. 拥有一个GitHub账户
2. 创建一个新的GitHub仓库（或使用现有仓库）
3. 确保仓库设置为公开（Public），或使用GitHub Pro账户

## 🚀 部署步骤

### 1. 准备仓库

1. 在GitHub上创建新仓库（例如：`my-secure-blog`）
2. 将本地代码推送到仓库：
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/your-username/your-repo-name.git
   git push -u origin main
   ```

### 2. 配置GitHub Pages

1. 进入仓库的 **Settings** 页面
2. 在左侧菜单找到 **Pages**
3. 在 **Source** 部分，选择 **GitHub Actions**
4. 保存设置

### 3. 配置仓库名称（重要）

如果您的仓库名不是 `my-secure-blog`，需要修改 `vite.config.js`：

```javascript
// vite.config.js
export default defineConfig({
  base: process.env.NODE_ENV === 'production' ? '/your-repo-name/' : '/',
  // ...
})
```

或者设置环境变量 `VITE_BASE_PATH`（GitHub Actions会自动设置）。

### 4. 触发部署

有两种方式触发部署：

#### 方式一：自动部署（推荐）
- 推送代码到 `main` 分支
- GitHub Actions会自动构建并部署

#### 方式二：手动触发
1. 进入仓库的 **Actions** 标签页
2. 选择 **Deploy to GitHub Pages** 工作流
3. 点击 **Run workflow** 按钮

### 5. 查看部署状态

1. 进入 **Actions** 标签页
2. 查看最新的工作流运行状态
3. 等待构建完成（通常需要2-5分钟）

### 6. 访问网站

部署完成后，访问：
```
https://[你的用户名].github.io/[仓库名]/
```

例如：`https://username.github.io/my-secure-blog/`

## ⚙️ 自定义配置

### 修改base路径

如果仓库名不同，修改 `.github/workflows/deploy.yml`：

```yaml
- name: Build
  run: npm run build
  env:
    NODE_ENV: production
    VITE_BASE_PATH: /your-repo-name/  # 修改这里
```

### 使用自定义域名

1. 在仓库 **Settings** → **Pages** 中设置自定义域名
2. 修改 `vite.config.js` 中的 `base` 为 `/`
3. 在域名DNS中添加CNAME记录指向 `username.github.io`

## 🔧 故障排除

### 构建失败

1. 检查 **Actions** 标签页中的错误信息
2. 确保 `package.json` 中有 `build` 脚本
3. 检查 Node.js 版本（需要 18+）

### 页面404错误

1. 检查 `vite.config.js` 中的 `base` 路径是否正确
2. 确保仓库名与base路径匹配
3. 清除浏览器缓存后重试

### 资源加载失败

1. 检查构建后的 `dist` 目录结构
2. 确保所有资源路径使用相对路径
3. 检查 GitHub Pages 的部署状态

## 📝 注意事项

- 首次部署可能需要几分钟时间
- 每次推送代码到 `main` 分支都会触发自动部署
- 部署是公开的，所有代码和构建产物都是可见的
- 建议使用 `.gitignore` 忽略敏感文件（如 `.env`）

## 🎉 完成

部署成功后，您的博客就可以通过GitHub Pages访问了！

